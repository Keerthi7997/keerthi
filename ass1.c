#include<stdio.h>
#include<math.h>
main()
{
class product{
		public int maxProduct(int[]nums){
			int maxA=num[0];
			int maxB=num[0];
			int min=num[0];
			for(int i=1;i<num,length;i++){
				int min=min*num[i];
				int maxB=maxB*num[i];
				min=math.min(math.min(num[i],min),maxB);
				maxB=math.maxB(math.maxB(nums[i],min),maxB);
				if(maxB>maxA)maxA=maxB;
			}
			return maxA;
		}
	}
}
